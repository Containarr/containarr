services:
  containarr:
    image: containarr
    container_name: containarr
    volumes:
      - ./volumes/containarr:/data
      - /var/run/docker.sock:/var/run/docker.sock
    network_mode: host
    tty: true
